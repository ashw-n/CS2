import java.util.ArrayList;

public class TeamBuilder
{
    public static ArrayList<ArrayList<String>> buildTeams(Student[] students, int teamSize, double minTotalGPA, int[][] conflicts)
    {
        // Student implementation

        //Terminate Early if: current size > max size, conflict detected, not enough students remaining, or if it cannot meet skill requirements
        
        //return new ArrayList<ArrayList<String>>();
    }
}